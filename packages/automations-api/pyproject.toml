[project]
name = "lintel-automations-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lintel-contracts",
    "lintel-domain",
    "lintel-api-support",
    "lintel-workflows",
    "fastapi>=0.115",
    "pydantic>=2.10",
    "croniter>=3.0",
]

[tool.uv.sources]
lintel-contracts = { workspace = true }
lintel-domain = { workspace = true }
lintel-api-support = { workspace = true }
lintel-workflows = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src/lintel/automations_api"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
