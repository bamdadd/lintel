"""Workflow command schemas.

Re-exported from lintel.contracts.commands — this module is the canonical import path
for workflow commands. The definitions live in contracts to avoid circular dependencies.
"""

from lintel.contracts.commands import StartWorkflow as StartWorkflow
