[project]
name = "lintel-workflows"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lintel-contracts",
    "lintel-agents",
    "lintel-observability",
    "lintel-sandbox",
    "lintel-skills-api",
    "structlog>=24.4",
    "langgraph>=0.3",
]

[tool.uv.sources]
lintel-contracts = { workspace = true }
lintel-agents = { workspace = true }
lintel-observability = { workspace = true }
lintel-sandbox = { workspace = true }
lintel-skills-api = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src/lintel/workflows"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
