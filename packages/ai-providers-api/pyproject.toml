[project]
name = "lintel-ai-providers-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lintel-contracts",
    "lintel-domain",
    "lintel-models",
    "lintel-persistence",
    "lintel-api-support",
    "fastapi>=0.115",
    "httpx>=0.28",
    "pydantic>=2.10",
    "structlog>=24.4",
]

[tool.uv.sources]
lintel-contracts = { workspace = true }
lintel-domain = { workspace = true }
lintel-models = { workspace = true }
lintel-persistence = { workspace = true }
lintel-api-support = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src/lintel/ai_providers_api"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
