[project]
name = "sample-project"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=9.0", "pytest-xdist>=3.8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
